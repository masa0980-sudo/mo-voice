# MO Voice の依存パッケージ（Windows専用）
# 各パッケージのライセンスは README.md を参照してください。

faster-whisper>=1.2.0   # MIT。音声認識
PyQt5>=5.15             # GPL v3。トレイ常駐・ダイアログ
pynput>=1.8             # LGPL v3。グローバルホットキー
PyAudio>=0.2.14         # MIT。マイク録音
pywin32                 # PSF。クリップボード・ウィンドウ操作
psutil                  # BSD-3-Clause。プロセス優先度・アクティブアプリ判定
numpy                   # BSD-3-Clause。音声データ処理

# 任意: 読みの表記ゆれ吸収（「クロードコードー」→「クロードコード」等）に使用。
# 未導入でも起動・動作します（該当機能が無効になるだけ）。
pykakasi                # GPL-3.0-or-later
